<%= form_for(@task_inventory) do |f| %>
  <% if @task_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_inventory.errors.count, "error") %> prohibited this task_inventory from being saved:</h2>

      <ul>
      <% @task_inventory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.radio_button :is_direct, true %>    
    <%= f.label :direct %>
    <%= f.radio_button :is_direct, false %>
    <%= f.label :indirect %>
  </div>
  <div class="actions">
    <%= f.submit "Create new task" %>
  </div>
<% end %>
