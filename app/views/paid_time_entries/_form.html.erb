<%= form_for(@paid_time_entry) do |f| %>
  <% if @paid_time_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paid_time_entry.errors.count, "error") %> prohibited this paid_time_entry from being saved:</h2>

      <ul>
      <% @paid_time_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %><br />
    <%= f.time_select :time %>
  </div>
  <div class="field">
    <%= f.label :effective_date %><br />
    <%= f.date_select :effective_date %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :team %><br />
    <%= f.text_field :team %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
